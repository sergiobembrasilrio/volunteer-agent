{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$CLAUDE_TOOL_INPUT\" | grep -qE 'git add.*(profile\\.md|data/|analyses/|messages/|assets/photos/|assets/cv/)'; then echo 'BLOCKED: tentativa de commitar dados pessoais (profile.md, data/, analyses/, messages/, assets/)'; exit 1; fi"
          }
        ]
      }
    ]
  }
}
